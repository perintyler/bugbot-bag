name: bugbot
description: Cursor Bugbot PR-review workflow — clean up, triage, and babysit bugbot findings

tools:
  entry: tools.ts

dependencies:
  - name: gh
    install: brew install gh
    reason: reads and manages GitHub review findings via the gh CLI
